export default function NewsList({data}) {
  return (
    <div className="component">
      <h2>NewsList</h2>
      {/* данный компонент отвечает за вывод списка новостей */}
      {/* props.data - массив объектов с новостями */}
    </div>
  )
}