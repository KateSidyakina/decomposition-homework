export default function Promo({src, title, link, text}) {
  return (
    <div className="component">
      <h2>Promo</h2>
      {/* данный компонент отвечает за вывод промо-объявления */}
      {/* для вывода заголовков используется компонент Link, данные для которого мы берём из props.title и props.link */}
    </div>
  )
}